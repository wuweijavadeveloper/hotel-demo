<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wuwei.gardemanager.mapper.StudentMapper" >
      <select id="getAll"  resultType="com.wuwei.gardemanager.entity.Student">
          select * from student
      </select>

    <insert id="addStu" parameterType="com.wuwei.gardemanager.entity.Student" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        insert into student
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="xh != null" >
                xh,
            </if>
            <if test="name != null" >
                name,
            </if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="xh != null" >
                #{xh,jdbcType=INTEGER},
            </if>
            <if test="name != null" >
                #{name,jdbcType=VARCHAR},
            </if>

        </trim>
    </insert>

    <delete id="delStu" parameterType="java.lang.Integer">
        delete from student where xh = #{xh}

    </delete>

    <update id="updateStu" parameterType="com.wuwei.gardemanager.entity.Student" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        update student
        <set >
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>

        </set>
        where xh = #{xh,jdbcType=INTEGER}
    </update>
    
    <select id="selectOne" resultType="com.wuwei.gardemanager.entity.Student">

      SELECT * from student where xh= #{xh}

    </select>

    <select id="selectOneByName" resultType="com.wuwei.gardemanager.entity.Student">

        SELECT * from student where name= #{name}

    </select>
</mapper>